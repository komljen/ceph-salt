{% set sensu_server = salt['mine.get']('roles:sensu-server','network.ip_addrs','grain').itervalues().next()[0] -%}

{
  "sensu": [
    {
      "name": "Sensu",
      "host": "{{ sensu_server }}",
      "ssl": "",
      "port": 4567,
      "user": "",
      "pass": "",
      "path": "",
      "timeout": 5000
    }
  ],
  "uchiwa": {
    "user": "{{ salt['pillar.get']('sensu:server:dashboard:uchiwa:user') }}",
    "pass": "{{ salt['pillar.get']('sensu:server:dashboard:uchiwa:password') }}",
    "port": {{ salt['pillar.get']('sensu:server:dashboard:uchiwa:port') }},
    "stats": 10,
    "refresh": 10000
  }
}
